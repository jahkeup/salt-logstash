{% from 'logstash/map.jinja' import logstash with context %}

input {
  redis {
    data_type => 'list'
    host => '{{logstash.redis_host}}'
    key => 'logstash'
  }
}

output {
  stdout { } # this outputs to service log
  elasticsearch {
    cluster => 'logstash'
  }
}